{
  "version": 2,
  "builds": [
    {
      "src": "index.js",
      "use": "@vercel/node",
      "config": {
        "maxLambdaSize": "50mb"
      }
    }
  ],
  "routes": [
    { "src": "/now", "dest": "index.js" },
    { "src": "/(.*)", "dest": "/public/$1" }
  ]
}