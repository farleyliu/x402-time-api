{
  "name": "x402-time-api",
  "version": "1.0.0",
  "description": "x402 get time api",
  "main": "index.js",
  "scripts": {
     "start": "node index.js",
    "vercel-build": "npm install && mkdir -p public",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
    "engines": {
    "node": "18.x"
  },
  "author": "farleyliu @x402",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "ethers": "^6.15.0",
    "express": "^5.1.0"
  }
}
